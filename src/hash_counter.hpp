#ifndef HASH_COUNTER_H
#define HASH_COUNTER_H
#include"hash_table.hpp"
#include<bits/stdc++.h>
using namespace std;


void count_kmers(segment sg, HashMap *hm, int tid);

uint64_t to_bit(string str);

#endif